= content_for :head do 
  = stylesheet_link_tag 'stories/index.css', :media => 'screen' 
.container
  %h1= "我的故事" 
  
  .my_stories
    - @stories.each do |s| 
      .story_item
        = link_to story_path(s), do
          %img{:src => s.image_medium_url} 
          %p=s.name

